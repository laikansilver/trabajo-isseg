interface ApplicationContext {
    name: string;
    projectName?: string;
    packageJsonName?: string;
}
/**
 * Returns the application name and any additional context that we need.
 */
declare function getApplicationContext(opts?: {
    appName?: string;
    packageRoot?: string;
}): ApplicationContext;

export { ApplicationContext as A, getApplicationContext as g };
