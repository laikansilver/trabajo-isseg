import { Plugin } from 'vite';
import { ToolbarPlugin } from './types.js';

interface VercelToolbarConfig {
    devServerPort?: number;
    plugins?: ToolbarPlugin[];
}
declare function vercelToolbar(config?: VercelToolbarConfig): Plugin;

export { VercelToolbarConfig, vercelToolbar };
