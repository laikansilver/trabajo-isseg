import { NextConfig } from 'next';
import { ToolbarPlugin } from './types.js';

interface VercelToolbarConfig {
    devServerPort?: number;
    enableInProduction?: boolean;
    plugins?: ToolbarPlugin[];
}
declare function withVercelToolbar(config?: VercelToolbarConfig): (nextConfig: NextConfig) => NextConfig;

export { VercelToolbarConfig, withVercelToolbar as default, withVercelToolbar };
