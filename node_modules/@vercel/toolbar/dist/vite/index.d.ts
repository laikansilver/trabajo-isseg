import { u as unmountVercelToolbar } from '../injection-eb01a000.js';

declare function mountVercelToolbar({ nonce }?: {
    nonce?: string;
}): typeof unmountVercelToolbar;

export { mountVercelToolbar };
